import io.jenkins.plugins.casc.ConfigurationAsCode;

node {
    stage('Apply config'){
        echo "Hello"
        git url: 'https://github.com/mukesh1011/jenkinscasc.git'
        //sh "ls -l"
        //sh "cp -r jenkins1/jenkins.yaml $JENKINS_HOME"
        ConfigurationAsCode.get().doExport(org.kohsuke.stapler.StaplerRequest req, org.kohsuke.stapler.StaplerResponse res)
    }
}
